import {createTheme} from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            main: "#356fc5"
        },
        secondary:{
            main: "#271f1f"
        },
        background:{
            paper: "#271f1f"
        }
    }
})

export default theme